define([], function() {

    function Spnr(spnr, user, uuid) {
        this.spnr = spnr;
        this.user = user;
        this.uuid = uuid;
    }

    Spnr.prototype.method_name = function(first_argument) {
        // body...
    };

    return Spnr

})
